{
  "name": "linkwarden",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/linkwarden/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/linkwarden",
        "main": "libs/linkwarden/src/index.ts",
        "tsConfig": "libs/linkwarden/tsconfig.lib.json",
        "assets": ["libs/linkwarden/*.md"]
      }
    },
    "publish": {
      "command": "node tools/scripts/publish.mjs linkwarden {args.ver} {args.tag}",
      "dependsOn": ["build"]
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/linkwarden/jest.config.ts"
      }
    }
  },
  "tags": []
}
